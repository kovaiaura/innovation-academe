# Roadmap

- [x] Stream large course PDFs through fresh signed URLs in every course viewer.
- [ ] Verify the stored 12 MB course PDF renders in the signed-in viewer (blocked until a preview user session is available).